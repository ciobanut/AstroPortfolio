---
const { href, className, type } = Astro.props
---

{
	href ? (
		<>
			<a href={href} class={className} >
				<slot />
			</a>
		</>
	) : (
		<>
			<button type={type} class={className}>
				<slot />
			</button>
		</>
	)
}
